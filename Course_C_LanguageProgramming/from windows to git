创建一个新文件夹

$git init #把它从文件夹变成git可以管理的仓库（通过生成.git文件确认）

将目标文件粘贴到文件夹内

$git add . #将目录下所有文件添加到仓库

$git commit -m "write something" #-m后为注释内容

C:/用户/.ssh/id_rsa.pub 内为SSH KEY，复制粘贴到github setting的SSH KEY内即可
若无id_rsa.pub，通过：
$ ssh-keygen -t rsa -C "youremail@example.com" #创建ssh key

$git remote add origin https://github.com/Ritetsu/xxxxx.git #关联本地仓库和git仓库，xxx为仓库名

$ git push -u origin master #第一次push加-u，之后不用加

【$ git pull --rebase origin master】#git仓库内含有本地没有的文件而报错时，进行远程同步（将git上的同步到本地）




